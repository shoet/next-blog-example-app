import { colors } from './colors'
import { fontSizes } from './fontSizes'
import { letterSpacings } from './letterSpacings'

export const theme = {
  colors,
  fontSizes,
  letterSpacings,
} as const
